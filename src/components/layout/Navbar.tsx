function Navbar() {
    return (
        <nav className="navbar">
            <h1>🔍 AI Search Engine</h1>

            <div>
                <button>⚙️</button>
            </div>
        </nav>
    );
}

export default Navbar;